# Example environment variables for local development
# Copy this file to .env and fill values (do NOT commit real secrets)

# Server
PORT=4300
SESSION_SECRET_LETTER=change_this_to_a_random_secret

# Database: choose one approach
# 1) Supabase / Postgres URL (recommended if you migrated to Postgres)
# DATABASE_URL=postgresql://postgres:your_password@db.host.supabase.co:5432/postgres

# 2) Or MySQL (if using the original MySQL code)
MYSQL_HOST=127.0.0.1
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=instagram_clone

# Mail (used for verification / notifications)
MAIL=youremail@example.com
MAIL_PASSWORD=your_mail_password

# Admin password used by admin routes (optional)
ADMIN_PASSWORD=admin

# Optional third-party keys
# Google geolocation API key used in some parts of the UI
GOOGLE_GEOLOCATION_KEY=your_google_api_key
